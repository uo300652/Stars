{
  "buildCommand": "npm run build --workspace=webapp",
  "outputDirectory": "webapp/dist",
  "installCommand": "npm install",
  "functions": {
    "api/**/*.ts": {
      "includeFiles": "server/data/**"
    }
  }
}
